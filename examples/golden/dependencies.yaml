apiVersion: sre.latent-sre/v1
kind: Dependencies
service: checkout
dependencies:
  - name: payments-api
    kind: http
    direction: downstream
    criticality: critical
    ownership: app
    runtimeBinding: observable-from-repo
  - name: postgres-main
    kind: datastore
    direction: downstream
    criticality: high
    ownership: platform
    runtimeBinding: not-observable-from-repo
provenance:
  repo: github.com/acme/checkout
  commit: 9d779ba
  scanDate: '2026-06-04T12:00:00Z'
  skill: map-dependencies
ownership: app
confidence: medium
needs-human-review: true
